# Rust-Practice


## First
```
# Installing Rust
curl https://sh.rustup.rs -sSf | sh 

source $HOME/.cargo/env

# Rechecking Installation
cargo --version
rustc --version

# Initializing a Project 
cargo init . # name of directory is project name
cargo init . ---lib

cargo new --help
cargo new <name>

# Getting Project Structure
tree
```